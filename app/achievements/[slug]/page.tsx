import { notFound } from "next/navigation"; import { achievements } from "@/src/data/achievements"; import { AchievementDetailPage } from "@/features/achievements/achievement-detail-page";
export function generateStaticParams(){return achievements.map(a=>({slug:a.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return {title:achievements.find(a=>a.slug===slug)?.title??"Achievement"}} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const achievement=achievements.find(a=>a.slug===slug);if(!achievement)notFound();return <AchievementDetailPage achievement={achievement}/>}
